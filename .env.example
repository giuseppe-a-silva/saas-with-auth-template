# Arquivo de Exemplo para Variáveis de Ambiente (.env)
# Copie este arquivo para .env e preencha com seus valores.

# URL de Conexão com o Banco de Dados PostgreSQL
# Formato: postgresql://<usuario>:<senha>@<host>:<porta>/<database>?schema=<schema>
DATABASE_URL="postgresql://user:password@localhost:5432/mydb?schema=public"

# Segredos JWT (Use valores fortes e aleatórios em produção!)
# Ferramenta online para gerar segredos: https://acte.ltd/utils/randomkeygen
JWT_SECRET="coloque_seu_segredo_jwt_aqui"
JWT_REFRESH_SECRET="coloque_seu_segredo_refresh_jwt_aqui"

# Tempos de Expiração dos Tokens
# s = segundos, m = minutos, h = horas, d = dias
ACCESS_TOKEN_EXPIRY="15m" # Exemplo: 15 minutos
REFRESH_TOKEN_EXPIRY="7d"  # Exemplo: 7 dias

# URL do Frontend (usado para configurações de CORS, etc.)
# Exemplo: http://localhost:3001 ou https://seu-dominio.com
FRONTEND_URL="http://localhost:3000"

# Ambiente da Aplicação (development, production, test)
# Influencia o modo secure do cookie e logs
NODE_ENV="development"

